# ============================================================
#  TitanicSinks – Requirements
#  These packages are installed inside the Airflow containers
#  via the _PIP_ADDITIONAL_REQUIREMENTS env var in docker-compose.yaml.
#
#  NOTE: apache-airflow itself is managed by the Docker image.
#        Do NOT install it separately here.
# ============================================================

# ── Machine Learning ──────────────────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.1

# ── MLflow ────────────────────────────────────────────────────
mlflow==2.11.1
boto3==1.34.69          # needed for MLflow S3 artifact store (optional locally)

# ── Database driver (Airflow ↔ PostgreSQL) ────────────────────
psycopg2-binary==2.9.9

# ── Airflow core (reference – managed by Docker image) ────────
# apache-airflow==2.9.2

# ── Utilities ─────────────────────────────────────────────────
requests==2.31.0
